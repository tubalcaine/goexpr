package bigfix

import (
	"fmt"
)

type BFServer struct {
	name string
	port string
	username string
	password string
}

func URL(srv BFServer) int, string {
	fmt.Println()
	return nil, "https://" + name + ":" + port
}
